version: "3.9"
services:
  db:
    image: postgres:16
    container_name: psl_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: psl
    ports:
      - "5432:5432"